# Definimos la función encriptadora

def encriptador():
    texto = input("Inserte el mensaje a encriptar: ")
    for a in texto:
        texto_encriptado = [range(len(texto))]

if __name__ == "__main__":
    encriptador()